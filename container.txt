converter = opencc.OpenCC('s2t')

speaker_prompt_text_transcription = converter.convert(
    cosyvoice.frontend.text_normalize_new(speaker_prompt_text_transcription, split=False)
)

content_to_synthesize = converter.convert(
    cosyvoice.frontend.text_normalize_new(content_to_synthesize, split=False)
)
