RUN pip install --upgrade "pip<25" "setuptools<70" wheel \
    && pip install \
        --index-url https://download.pytorch.org/whl/cu118 \
        torch==2.3.1+cu118 \
    && pip install --no-build-isolation -r /tmp/bz_requirements.txt
